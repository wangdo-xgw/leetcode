一、git忽略.idea/ workspace.xml
      1、先清除.idea的git缓存
      git rm -r --cached .idea

      2、添加.gitignore文件，内容如下：
      .idea/*

      3、终端执行如下命令：
      git add .
      git commit -m 'add .gitignore'
      git push